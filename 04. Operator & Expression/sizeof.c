//wap to print the size of float, int, char, and double type variables
#include "stdio.h"
int main(){
  int x;
  float y;
  char z;
  double a;
  printf("size of x is %d\n",sizeof(x));
  printf("size of y is %d\n",sizeof(y));
  printf("size of z is %d\n",sizeof(z));
  printf("size of a is %d\n",sizeof(a));
}